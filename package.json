{
  "name": "transcribr",
  "version": "0.3",
  "description": "",
  "keywords": ["NLP", "Russian"],
  "license": "MIT",
  "author": "Andrei K.",
  "scripts": {
    "build": "parcel build index.html",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
